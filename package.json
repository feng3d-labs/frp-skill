{
  "name": "@feng3d/frp-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "frp 内网穿透 npm 包集合",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wardenfeng/frp-skill.git"
  },
  "author": "feng3d",
  "license": "MIT"
}